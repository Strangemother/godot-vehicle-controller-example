extends Camera

func _process(delta):
	pass #look_at(get_parent().get_node("Rocket").transform.origin,Vector3(0,1,0))

#func _input(event):
#	if Input.is_action_pressed("Reset"):
#		get_tree().reload_current_scene()
